name: flutternew
description: "A new Flutter project."

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  velocity_x: ^4.0.0
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.6
  provider: ^6.0.0
  firebase_core: ^2.7.0  # Firebase core for initialization
  firebase_auth: ^4.8.0  # Firebase authentication for OTP

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/files/
